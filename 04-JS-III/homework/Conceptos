// Arrays: Un array o matriz es simplemente una variable que puede contener valores múltiples, a diferencia de una variable regular que solo puede contener un único valor.
Tambien un array es una colección de variables del mismo tipo, a la que se hace referencia por un nombre común.